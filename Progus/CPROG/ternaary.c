#include <stdio.h>
int main()
{
	int x=1;
	int a=5, b=1;
	int sum = 0;
	int y;// = (x==(a==5 ? (b==1?5:7):6)) ? (a==5 ? b+a: 9) : 0;
	for(
	printf("x = %d\n", x);
	printf("y = %d\n", y);
	//printf("%d\n", a==b);
	return 0;
}
